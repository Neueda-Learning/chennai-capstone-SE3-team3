info:
  name: Account Orders
  type: http
  seq: 6

http:
  method: GET
  url: accounts/1001/orders
  auth:
    type: bearer
    token: "{{JWT_KEY}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
