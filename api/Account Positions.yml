info:
  name: Account Positions
  type: http
  seq: 5

http:
  method: GET
  url: accounts/1001/positions
  auth:
    type: bearer
    token: "{{JWT_KEY}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
